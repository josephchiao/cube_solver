// main.rs will deal with the CLI (command line interface) for interacting with the functionality of the cube solver
// functionality is added by the other .rs files in the src folder

fn main() {
    println!("Hello, world!");
}
